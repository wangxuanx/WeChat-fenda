<view class="about">
	<view class="master-info">
		<image src="/img/avatar/{{userInfo.avater}}" wx:if="{{userInfo.avater}}"></image>
		<view class="master-brief">
			<view class="master-user">{{userInfo.name}}</view>
			<view class="master-fans"><text class="bold">{{userInfo.fans}}</text> 收听</view>
		</view>
		<view class="master-follow">收听</view>
	</view>
	<view class="master-intro">{{userInfo.intro}}</view>
	<view class="detail">
		<view>{{userInfo.detail}}</view>
	</view>
</view>
<view class="quiz-wrapper">
	<view class="quiz-info">公开提问</view>
	<view class="quiz-input">
		<textarea placeholder="向TA提问，等TA语音回答；超过48小时未回答，将按支付路径全额退款，被回答后可免费向TA追问" maxlength="5">向TA提问，等TA语音回答；超过48小时未回答，将按支付路径全额退款，被回答后可免费向TA追问</textarea>
	</view>
	<view class="quiz-cost">{{userInfo.quizCost + "￥"}}</view>
	<button class="submit">写好了</button>
</view>